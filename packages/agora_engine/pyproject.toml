[project]
name = "agora-engine"
version = "0.1.0"
description = "YH010/YH010-g 共有エンジン: DecisionMatrix / FactorModel / Intervention / テープ規約 (YH010_HANDOFF §7 が正本)"
requires-python = ">=3.11"
dependencies = ["numpy"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["agora_engine"]
