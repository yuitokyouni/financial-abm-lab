[project]
name = "abm-models"
version = "0.1.0"
description = "金融ABMモデルの正準実装 (SG ほか)"
requires-python = ">=3.11"
dependencies = ["numpy"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["abm_models"]
