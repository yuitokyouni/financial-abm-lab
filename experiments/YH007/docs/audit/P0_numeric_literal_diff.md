# P0 是正 6 コミットの numeric literal 差分(マージ条件 5)

- 実施日: 2026-08-13
- 対象範囲: `746b854..849bdad`(P0 是正の全 6 コミット)+ 本コミットの条件 3 対応
- 方法: `git diff 746b854..849bdad -- "*.py"` の数値を含む全変更行(112 行)を目視分類。
  `.md` / `.json` の数値変更は監査レポート・artifact の内容そのものなので対象外
  (spec の訂正注記は履歴行を書き換えない形式であることを別途確認)。

## 分類結果

| # | 分類 | 箇所 | 判定 |
|---|---|---|---|
| 1 | φ/σ: 0.418(0.42) → 0.615、6e-3 → 3.81e-3 | model.py 8 スロット / zi_agent.py fallback / スクリプト 4 本の呼び出し点 / テスト fixture 3 箇所(does_not_diverge, agg_band, p3d×2)/ docstring・コメント | **承認済み変更そのもの**(ユーザ裁定「更新漏れ」) |
| 2 | 同値の新規リテラル: 0.615 / 3.81e-3 | `test_yh007_8_calibration_values.py` の期待定数、p3d `--phi-ar1`/`--sigma-ar1-abs` の CLI 既定値 | #1 の regression guard / override 経路。値は #1 と同一 |
| 3 | **テスト構成の変更**(`test_matched_ar1_agg_rate_within_band`) | n_zi 20 → 10+10(2-group 化)、margin 3e-5..1e-4 → 戦略 2.5e-5..1.2e-4 + 流動性 2.0e-5..6.0e-5、sigma_eval=5e-5 追加、測定対象を全 agent → 戦略群のみに変更 | **リテラルの閾値変更ではないが、判定の意味が変わった唯一の箇所**。新構成は p3d スクリプトの本体構成と同一値(独自の数値は導入していない)。旧 1-group 構成の崩壊(agg=0.0015)は記録済み |
| 4 | assert 閾値 | `0.02 < agg_rate < 0.40`(緩和帯)、`drift_abs < 0.5` / `< 1.0`、`abs(phi_est−target) < 0.2` 等 | **6 コミット中の変更なし**(全て既存値のまま) |

**結論: 6 コミットに閾値リテラルの緩和・変更は存在しない。** 数値変更は
(1) 承認済みの φ/σ 統一と (2) その同値の guard/override、(3) agg テストの
構成変更(値は本体スクリプトからの転記のみ)に尽きる。

## 発見と対応(条件 3)

唯一の閾値問題は **6 コミットより前から存在した緩和**: `agg_rate` の assert が
0.02..0.40 で、docstring・spec §3.1 の目標帯 [0.05, 0.20] より広い(初版
aa6d299 から)。#3 の構成変更はこの緩和帯を無変更で温存していた。

→ **本コミットで [0.05, 0.20] へ厳格化**(マージ条件 3)。修正較正 + 2-group
構成の実測 agg = 0.079(seed 42, 400 steps)で帯内、テストは厳格化後も GREEN。

## 残る注意(緩和ではないが記録)

- #3 の測定対象変更(全 agent → 戦略群)は、2-group 化に伴う必然(流動性役の
  約定を混ぜると parity の定義が変わる)だが、旧テストと数値的に比較可能では
  ない。旧構成の値は §2(`P0_yh007_recalibration_rerun.md`)に記録済み。
- p3d の CLI override 既定値(0.615/3.81e-3)は #1 と同値だが、定義箇所が
  1 つ増えた(呼び出し点ハードコード → CLI 既定値)。較正定数の入力
  アーティファクト化(BACKLOG の Contract 要件 1)が入るまでは、値変更時に
  ここも追随が必要な箇所として `test_yh007_8_calibration_values.py` の対象外に
  ある点に注意(スクリプトの CLI 既定値はソース検査テストの対象に含まれていない)。
