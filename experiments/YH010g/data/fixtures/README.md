# 検証入力

`validation_groundtruth.csv` は旧 `docs/yh010g_validation_groundtruth.csv` を内容不変で移動したもの。
ラベルを推測で補完しない。`validation_targets.csv` と `manual_attributes.csv` は文書中の運用例であり、
この整理では作成していない。存在しない入力を検証済みと扱わない。
