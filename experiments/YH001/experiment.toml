# 配置台帳。研究結果の合否や再現完了を意味しない。
id = "YH001"
title = "Cont–Bouchaud"
kind = "simulation"
shared_packages = ["abm_models", "stylized_facts"]
paths = ["baseline.py"]
commands = ["uv run python -m experiments.YH001.baseline --seed 42"]
archives = ["imported/speculation-game-info/experiments/YH001"]
