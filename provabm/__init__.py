"""provabm — PROV-ABM 捕捉/再現性レイヤー(v0: L2 minimum)。

`ctx.*` API、reach 四分割(v0 は `reported` のみ)、capture L0-L2、
claim↔reach の最小 validator、L2 provenance(prov.json)を提供する。
L3+(AST whitelist / taint / restricted DSL)は protocol stub のみ。
"""

__version__ = "0.1.0"
